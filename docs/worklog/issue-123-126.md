# Issue #123/#124/#125/#126 UX・運用バックログ4件の登録

### 2026-07-06 Issue #123〜#126 バックログ登録のレビュー

- 担当: reviewer
- ブランチ: docs-plan-add-ux-backlog
- 内容: Issue #123/#124/#125/#126 の起票と `docs/PLAN.md` バックログへの
  追加（コミット 3afc686、docs のみの変更）をレビューし、合格とした
- 確認したこと:
  - `gh issue view` で4件すべてが OPEN であり、各タイトルが PLAN.md の
    バックログ記載（未チェック項目）と一致すること
  - Issue 本文の技術的主張をコードで検証した:
    - #125: `packages/frontend/src/entities/infraNode.ts` の
      `DEFAULT_GRID` は `gapX: 260` で、記載どおりカード幅とほぼ同じ。
      `MIN_PULSE_DURATION_MS = 450` も
      `packages/frontend/src/entities/blockPulse.ts` の実装と一致
    - #126: `scripts/dev-down.sh --docker` は `docker compose down` を
      呼ぶのみで、動的追加コンテナの削除処理は無い。対応方針にある
      `com.chainviz.managed` ラベルは collector が実際に付与している
      （`packages/collector/src/index.ts` の回収処理で使用中）
  - ラベルの妥当性: #123/#124/#125 は `frontend` で妥当（#125 の原因箇所
    `infraNode.ts` も frontend パッケージ）。#126 は `node-env`。対象の
    `scripts/dev-down.sh` はリポジトリ直下のスクリプトで、厳密には
    どのパッケージラベルの説明（node-env =「profiles/ 配下のノード環境」）
    にも該当しないが、作業内容は Docker コンテナのライフサイクル管理で
    あり、担当・ドメインの近さから `node-env` を最も近い選択として容認した
  - `pnpm lint` が通ること（終了コード 0）
  - コミット粒度: docs のみの1コミット（3afc686）で問題なし
- 指摘（非ブロッキング）: #123/#124/#125 の対応方針が参照する
  `chainviz-ux` エージェントの定義ファイル
  （`.claude/agents/chainviz-ux.md`）は、メインの作業ディレクトリには
  存在するが git にはまだコミットされていない。これらの Issue に着手する
  前にコミットしておくこと
